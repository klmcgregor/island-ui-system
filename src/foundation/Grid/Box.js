import React from 'react';
import styled from 'styled-components';
import {
  space,
  layout,
  flexbox,
  border,
  position,
  typography,
  color,
} from 'styled-system';

const Box = styled('div')(
  space,
  layout,
  flexbox,
  border,
  position,
  typography,
  color,
);

export default Box;